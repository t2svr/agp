export default {
  plugins: [
    {
      name: 'preset-default',
      params: {
        overrides: {
          removeDesc: {
            removeAny: true
          }
        },
      },
    },
  ],
};